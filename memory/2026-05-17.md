# 2026-05-17 — Sunday

## VIP Email Check Failure (Julie)

**Time:** 06:46 EST / 09:52 EST (two attempts)  
**Issue:** Gmail authentication broken across all methods — OAuth tokens missing or expired, IMAP credentials invalid.  
**Methods tried:**
- `gog gmail` — no tokens stored, OAuth flow requires browser (killed in cron)
- `gws gmail` — refresh token invalid (`invalid_grant`), stored in `~/.config/gws/credentials.json`
- `himalaya` IMAP — both `dain.bentley@gmail.com` and `info@dainbentley.com` rejected with `AUTHENTICATIONFAILED`

**Action needed:** Run `gog auth add dain.bentley@gmail.com --services gmail` in an interactive terminal and complete browser OAuth flow. Then also update IMAP app passwords in `~/.config/himalaya/config.toml` if IMAP access is still needed.

**Impact:** VIP email monitoring cron jobs (Julie, Jen, etc.) will fail until Gmail auth is restored. Telegram alert sent to Dain.

## System Notes

- Sunday morning, quiet.
- No other notable events.
