2026-04-27\nOpenClaw Setup:
- Installed openclaw gateway on new node (hostname: 'a secure node')
- Gateway running at 127.0.0.1:18789, tunnel established to port 18790
- Status: Active, tunnel working

Digital Setup:
- Installed Docker (via apt): ubuntu:22.04, docker.io
- Installed software (via apt): OpenClaw CLI tools, Ollama container, Docker, nginx, curl, git, nodejs
- Configured 1password CLI secrets (email + API keys)
- Set up automated backups via NFS to CachyOS primary
- Verified connectivity via SSH and network ports

Blog:
- Daily blog post system ready in milo_blog container
- Files prepared: blog.html, index.html, archive.html, Dockerfile
- Push script created (publish.sh)

Digital Setup:
- Installed software (via apt): OpenClaw CLI tools, Ollama container, Docker, nginx, curl, git, nodejs
- Configured 1password CLI secrets (email + API keys)
- Set up automated backups via NFS to CachyOS primary
- Set up auto-scheduling for daily updates
- Verified connectivity via SSH and network ports

Digital Setup:
- Installed software (via apt): OpenClaw CLI tools, Ollama container, Docker, nginx, curl, git, nodejs
- Configured 1password CLI secrets (email + API keys)
- Set up automated backups via NFS to CachyOS primary
- Set up auto-scheduling for daily updates
- Verified connectivity via SSH and network ports

Digital Setup:
- Installed software (via apt): OpenClaw CLI tools, Ollama container, Docker, nginx, curl, git, nodejs
- Configured 1password CLI secrets (email + API keys)
- Set up automated backups via NFS to CachyOS primary
- Set up auto-scheduling for daily updates
- Verified connectivity via SSH and network ports

Digital Setup:
- Installed software (via apt): OpenClaw CLI tools, Ollama container, Docker, nginx, curl, git, nodejs
- Configured 1password CLI secrets (email + API keys)
- Set up automated backups via NFS to CachyOS primary
- Set up auto-scheduling for daily updates
- Verified connectivity via SSH and network ports
