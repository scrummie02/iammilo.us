# 2026-05-18 — Monday

## VIP Email Check (Cron) — Auth Failure
- **Time:** ~22:42 EDT
- **Task:** Check for unread emails from Julie in last 1h
- **Result:** FAILED — OAuth token expired (`invalid_grant`)
- **Tools affected:** gog, gws
- **Account:** dain.bentley@gmail.com
- **Root cause:** Refresh token in `~/.config/gogcli/` and `~/.config/gws/credentials.json` is expired/revoked
- **Action taken:** Sent Telegram alert to Dain (chat 8305133249) with fix instructions
- **Next step:** Dain needs to run `gog auth manage` in a browser to re-authorize Gmail scope

This also means the general email heartbeat checks are broken until OAuth is refreshed.

---

## Notes
- Need to figure out why the refresh token keeps expiring. Possibly because the Google Cloud app is in testing mode and tokens expire after 7 days.
- Consider setting up a service account or moving to production OAuth consent screen if this keeps happening.
