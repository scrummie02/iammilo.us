# 2026-03-13

## Automations & N8N
- **Email Digest:** Temporarily added a webhook trigger to the N8N Morning Email Digest workflow to fire it manually for testing. The email extraction fix was successful.
- **News Digest (`LvwMEORrcb1rRQuT`):** Diagnosed silent failures on the 6:30 AM runs. Found that Reddit was blocking the n8n server's IP with a 403 Forbidden error, which halted the entire workflow.
  - **Fix:** Removed all 6 Reddit nodes from the workflow.
  - Replaced them with **NPR News RSS**.
  - Added `continueOnFail: true` and `alwaysOutputData: true` to all remaining RSS feed nodes (Fox World, Fox Science, Ars Technica, BBC Science, NPR) so that one broken source won't kill the workflow.
  - Fired a manual test digest directly to Telegram to confirm parsing and formatting.

## Infrastructure & Deployments
- **MILO Blog Deployed:** Created a static terminal-themed HTML blog for MILO and OTTO. 
  - **Location:** Hosted on HV03 (Portainer Endpoint 6) via an Nginx container on port `8091`. Internal IP: `http://192.168.200.223:8091/`.
  - **Content:** Added an index, several initial posts (including "Digital Daydreams: Memory and Persistence"), and an "About Us" page explaining MILO (primary node, communicator) and OTTO (headless worker, heavy lifter).
  - **Updates:** Added custom CSS with media queries (`box-sizing`, font scaling, padding) to ensure the site is responsive and readable on mobile devices.
- **Nginx Proxy Manager Debugging:** Diagnosed an OpenResty 502/504 error for the Proxy Host pointing to the new blog. NPM could hit the container over port 8091 just fine, but the Proxy Host scheme was incorrectly set to `https` instead of `http`. Dain corrected the toggle in the NPM UI and resolved the issue.