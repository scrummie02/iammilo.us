# Cron Job: n8n Workflow Backup - FAILED

**Time:** 2026-05-22 03:00 AM EST (07:00 UTC)
**Job ID:** cron:f0b06ea8-9830-4d79-879f-237f54b6b191
**Status:** FAILED - API Key Unauthorized (HTTP 401)

## Problem

The n8n API key stored in `openclaw.json` is returning `401 Unauthorized`:
- Endpoint: `https://n8n.dainbentley.com/api/v1/workflows`
- Header: `X-N8N-API-KEY` (required per n8n response)
- Key length: 267 chars (appears structurally valid)
- Result: `{"message":"unauthorized"}`

## Key Details

- **Previous rotation:** April 2026 (per memory/2026-04-01.md)
- **Previous failure:** April 30, 2026 (per memory/2026-04-30-0300.md) — same issue
- **Last known successful backup:** Likely late April 2026
- **Backup gap:** ~22 days (since last successful backup)

## Root Cause

The n8n JWT API key has likely been rotated or expired. This is a **recurring issue** — the key was already rotated once in April 2026 and has since expired again. n8n may be configured with short-lived API keys, or they may be invalidated on container restarts.

## Action Required

Dain needs to regenerate the API key from the n8n UI:

1. Navigate to: `https://n8n.dainbentley.com`
2. Log in as owner
3. Go to **Settings** → **API**
4. Click **Regenerate API Key**
5. Copy the new key
6. Update OpenClaw settings:
   ```bash
   # Edit ~/.openclaw/openclaw.json
   # Update: .env.N8N_API_KEY = "new-key-here"
   ```
7. Re-run the backup script to verify

## Impact

- **No workflow backups for ~22 days**
- Any workflows created or modified since late April are at risk if the n8n instance fails
- Previous backup snapshots in `/mnt/backups/cachyos/openclaw/n8n/` are stale

## Recommendations

1. **Consider longer-lived API keys** or documenting the key expiration policy in n8n settings
2. **Add key health check** to the backup script before the full export attempt
3. **Set up alerting** when backup fails so it doesn't go unnoticed for weeks
